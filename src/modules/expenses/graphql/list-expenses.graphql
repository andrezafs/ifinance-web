query ListExpense($filter: ListExpenseFilter!) {
  listExpense(filter: $filter) {
    amount
    expenses {
      category {
        name
        id
      }
      name
      value
      purchaseDate
      id
      creditCard {
        name
      }
    }
  }
}
