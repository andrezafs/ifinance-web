query ListExpensesByWallet($filter: ListExpenseFilter!) {
  listExpenseByWallet(filter: $filter) {
    expenses {
      category {
        name
        id
      }
      name
      value
      purchaseDate
      id
      isIgnored
      creditCard {
        name
      }
    }
  }
}
